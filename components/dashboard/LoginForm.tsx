"use client";
import Image from "next/image";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});setLoading(false);if(!response.ok){setError("Connexion impossible. Vérifiez les identifiants administrateur.");return}router.push("/dashboard");router.refresh()}return <main className="admin-login"><form onSubmit={submit}><Image src="/images/linuzvision-logo.jpg" alt="LinuZvision LLC" width={320} height={115} priority/><p>CONTENT OPERATIONS</p><h1>ADMINISTRATION</h1><label><span>EMAIL</span><input name="email" type="email" autoComplete="username" required/></label><label><span>PASSWORD</span><input name="password" type="password" autoComplete="current-password" required/></label><button type="submit" disabled={loading}>{loading?"CONNECTING…":"SIGN IN"}</button><p className="admin-error" role="alert">{error}</p><a href="/">RETURN TO WEBSITE</a></form></main>}
